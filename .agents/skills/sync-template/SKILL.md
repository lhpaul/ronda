---
name: sync-template
description: Command-style Codex alias for syncing framework updates from the upstream template. Use when the user asks for /sync-template.
---

# Sync Template

This is the Codex command-style alias for Claude Code `/sync-template`.

1. Read `AGENTS.md` for repository-wide rules.
2. Read `.codex/skills/workflow-sync-template/SKILL.md`.
3. Follow the `workflow-sync-template` skill exactly, including role-aware
   manifest selection for `single_repo`, `workflow_hub`, and `product_repo`
   repositories.
