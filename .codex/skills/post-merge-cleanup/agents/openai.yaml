interface:
  display_name: "Post-merge cleanup"
  short_description: "Fetch, develop, pull, delete merged branch; update issue tracker."
  default_prompt: "Run post-merge cleanup, preserving workflow_hub selected product repository context for product-owned implementation cleanup, then update the related issue tracker item when applicable."

policy:
  allow_implicit_invocation: false
