interface:
  display_name: "Workflow Code Reviewer"
  short_description: "Review implemented changes and surface risks first."
  default_prompt: "Use $workflow-code-reviewer to review current implementation changes, reporting whether the artifact is hub-owned or product-repository-owned."

policy:
  allow_implicit_invocation: true
