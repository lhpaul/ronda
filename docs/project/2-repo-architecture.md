# Repository Architecture

> **STATUS: PLACEHOLDER**
>
> This document is generated by the project setup agent (`docs/workflow/setup/protocol.md`).
> Run the setup workflow to populate it, or fill it in manually following the structure below.

---

## Overview

> Briefly describe the repository structure (monorepo, polyrepo, single app, etc.) and the rationale.

## Directory Structure

> Show the top-level structure and explain what each directory contains.

```
[repo-root]/
├── [directory-1]/    # [description]
├── [directory-2]/    # [description]
└── [directory-3]/    # [description]
```

## Applications / Services

> List each deployable unit (app, service, function) with its purpose, tech stack, and entry point.

| Name    | Purpose       | Stack  | Entry point |
| ------- | ------------- | ------ | ----------- |
| [App 1] | [Description] | [Tech] | [Path]      |

## Shared Packages / Libraries

> List shared packages/libraries with their purpose and consumers.

| Package     | Purpose       | Consumed by    |
| ----------- | ------------- | -------------- |
| [Package 1] | [Description] | [App 1, App 2] |

## Dependency Graph

> Optionally, show how packages/apps depend on each other.

```
[App 1] → [Package A]
[App 2] → [Package A], [Package B]
```

## Common Commands

> List the most important commands for development, build, test, and deploy.

```bash
# Install dependencies
[command]

# Start development
[command]

# Build
[command]

# Test
[command]

# Lint / Format
[command]

# Deploy
[command]
```

## Environment Setup

> How to set up a local development environment from scratch. Reference `docs/project/3-software-architecture.md` for stack details.
