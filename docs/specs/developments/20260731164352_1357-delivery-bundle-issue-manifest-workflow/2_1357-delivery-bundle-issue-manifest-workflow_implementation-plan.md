# Delivery Bundle Issue And Manifest Workflow - Implementation Plan

**Spec**:
[`1_1357-delivery-bundle-issue-manifest-workflow_specs.md`](./1_1357-delivery-bundle-issue-manifest-workflow_specs.md)
**Smoke test runbook**:
[`docs/testing/workflow/1357-delivery-bundle-issue-manifest-workflow.smoke-test.md`](../../../testing/workflow/1357-delivery-bundle-issue-manifest-workflow.smoke-test.md)

---

## Summary

**Approach**: Add a hub-owned delivery bundle manifest helper that creates,
updates, inspects, removes components from, and finalizes a versioned manifest
using `component_release_evidence.v1` records from #1356. Keep the feature in
the workflow hub: bundle issue metadata, manifest revisions, audit events, and
finalization checks live in the hub, while product release artifacts remain
owned by their selected product repositories.

**Estimated complexity**: M

**Rationale**: The implementation is bounded to workflow shell tooling,
fixtures, tests, and documentation, but it must preserve atomic revision
semantics and fail closed on stale or conflicting structured evidence.

**Dependencies**: #1353 and #1356 are merged through implementation. #1358 and
#1359 are downstream or orthogonal and must not be pulled into this scope.

---

## Verification Log

| Check | Command / query | Result |
| --- | --- | --- |
| Repo revision | `git rev-parse --short HEAD` | `bc5411b` |
| Template-fit check | `rg -n "template:\|is_template" .ai-dev-workflow.yaml` | `.ai-dev-workflow.yaml` declares `template.is_template: true`; this item is generic workflow tooling and documentation. |
| Same-surface PR check | `gh pr list --base develop-multi-repo-releases --state open --search "1357" --json number,title,headRefName,baseRefName,state,url` | No open #1357 PR existed before this plan branch mutation. |
| Existing issue-template surface | `find .github -maxdepth 3 -type f \| sort` | The repository currently has GitHub Actions workflows but no issue-template files; #1357 should introduce the first `.github/ISSUE_TEMPLATE/` file deliberately. |
| Component release evidence surface | `rg -n "component_release_evidence\|release_outcome\|cleanup_outcome\|hub_tracker_ref\|contract_revision" scripts/development-workflow docs/workflow/development-workflow docs/testing/workflow -g '*.sh' -g '*.md'` | Existing #1356 runtime and tests define `component_release_evidence.v1`, stable identity fields, release outcome fields, cleanup outcome fields, and hub tracker references that #1357 consumes. |
| Existing workflow test harness | `find scripts/development-workflow/tests -maxdepth 1 -type f -name 'test-*.sh' \| sort` | Shell test coverage already includes component release evidence, workflow hub behavior, release cleanup, and run-epic helpers; add one focused delivery-bundle manifest test file. |
| Implementation validation requirements | `rg -n "markdownlint-cli2\|markdown-heuristic-lint.py\|workflow-shell-snippet-lint.py\|shellcheck" AGENTS.md scripts/development-workflow docs/workflow/development-workflow` | Existing repo guidance requires standard Markdown linting, heuristic Markdown linting, workflow shell snippet linting, and shell linting for shell changes; keep these explicit in the implementation checklist. |

---

## Cross-Cutting Operational Assumption Check

### Applicable

| Assumption surface | Recorded value | Authoritative source | Verified at | Bounded cross-check scope | Result |
| --- | --- | --- | --- | --- | --- |
| Approved plan base | `develop-multi-repo-releases` | `/run-epic 1352` invocation policy, branch guard, and merged #1357 spec PR #1412 | 2026-07-31T21:49:25Z, repo `bc5411b` | Epic #1352 child #1357 and same-base open PR query for #1357 only | `Verified` |
| Artifact owner | Hub repository owns delivery bundle issues, manifests, audit events, and finalization; product repositories own component release artifacts | #1353 release artifact ownership contract, #1356 component release evidence contract, and #1357 accepted spec | 2026-07-31T21:49:25Z, repo `bc5411b` | #1357 plan branch plus #1356 evidence helper references | `Verified` |
| Downstream item boundary | #1358 milestone reconciliation and #1359 adoption or assurance behavior stay out of #1357 | Confirmed #1357 issue comment and accepted spec-dispatch context | 2026-07-31T21:49:25Z, repo `bc5411b` | Epic #1352 remaining children #1357, #1358, and #1359 only | `Verified` |

---

## Layer-by-Layer Changes

### Database / Data Layer

- [ ] No database, migration, or persistent seed-data changes. Delivery bundle
      manifests are file-based workflow evidence generated by the helper.

### Backend / API

- [ ] No HTTP API changes.

### Shared Packages / Libraries

- [ ] Add `scripts/development-workflow/delivery-bundle-manifest.sh` as the
      canonical hub-owned delivery bundle manifest helper. It should support
      subcommands equivalent to `create`, `add-component` or
      `update-component`, `inspect`, `remove-component`, and `finalize`. Map to
      AC1-AC15.
- [ ] Define `delivery_bundle_manifest.v1` as a machine-readable JSON object
      with immutable `bundle_key`, bundle metadata, lifecycle status, current
      revision, declared components, removed component history, readiness data
      tied to the current revision, finalization data, and append-only audit
      events. Map to AC2, AC5, AC8, AC9, and AC13.
- [ ] Define the manifest-targeting contract for every helper command. The CLI
      must require `--manifest PATH` and `--bundle-key KEY` for mutating and
      inspection commands. `--manifest` selects the hub-owned evidence file;
      `--bundle-key` is the immutable logical delivery identity stored in the
      manifest. Commands must reject a missing key, a malformed key, or a key
      that does not match the manifest contents before any mutation. Map to AC1,
      AC2, AC6, and AC13.
- [ ] Implement atomic update semantics for every accepted mutation. Hold an
      inter-process exclusive lock from manifest read through replacement,
      including new-manifest creation. Recheck the expected current revision
      while the lock is held, detect idempotent replay from the locked current
      state, compute the new state, increment the revision, append the audit
      event, create a temporary file in the manifest directory using exclusive
      creation, validate the staged JSON, and then replace the manifest. Retry
      idempotent reapplication only after a fresh locked read. Stale or
      conflicting writes must leave the prior manifest unchanged. Map to AC3,
      AC4, AC6-AC9, and AC13.
- [ ] Consume `component_release_evidence.v1` records without rewriting them.
      Preserve stable identity fields from the evidence:
      `selected_product_repo_key`, `canonical_repository_identity`,
      `release_correlation_key`, and `contract_revision`. Use those fields for
      deduplication and conflict detection instead of display names or branch
      names. Map to AC3, AC4, AC6, and AC15.
- [ ] Normalize the component version/tag contract in the manifest schema and
      helper interface. Component evidence attachment must provide a
      `component_tag` for every release-backed component and may also provide
      `component_version` when the product has a separate semantic version.
      Finalization must require `component_tag` on every declared current
      component and must preserve `component_version` as optional descriptive
      metadata. Deduplication and conflict checks must include `component_tag`
      and must reject attempts to replace an accepted tag for the same stable
      component identity without an explicit fresh update path. Map to AC3,
      AC6, AC10, and AC13.
- [ ] Validate finalization outcomes exactly as the spec requires:
      `component_release_routed`, `completed`, acceptable CI, acceptable
      deployment, `complete` cleanup, acceptable hub tracker reconciliation, and
      released or merged child state. Failed, blocked, pending, missing,
      conflicting, or stale states block finalization. Map to AC10-AC14.
- [ ] Treat add, update, and removal after `ready_to_finalize` as readiness
      invalidation for the current revision. The helper should transition the
      bundle back to `open` or `blocked` and require inspection or finalization
      to recompute readiness. Map to AC8 and AC9.
- [ ] Emit stable text and JSON inspection output naming bundle status,
      revision, component evidence state, blockers, next action, and whether
      finalization is allowed. Map to AC5 and Operational Visibility.

### Frontend / UI

- [ ] No frontend or browser UI changes.

### Infrastructure / Configuration

- [ ] Add `.github/ISSUE_TEMPLATE/delivery-bundle.yml` or the repository's
      matching GitHub issue-template format. The template must collect delivery
      title, purpose, parent epic or request, participating product components,
      known child items, finalization owner, and optional rollout notes. Map to
      AC1.
- [ ] Do not add new secrets, environment variables, shared suite versions,
      shared release branches, tags, or GitHub Release behavior. Map to AC14.

### Documentation / Workflow Guides

- [ ] Update `docs/workflow/development-workflow/repository-modes.md` to
      describe the delivery bundle manifest as the hub-owned, authoritative
      component-version record for coordinated deliveries.
- [ ] Update `docs/workflow/development-workflow/protocols/05-prepare-release-protocol.md`
      to explain how product release evidence can be attached to an existing
      delivery bundle without changing product release ownership.
- [ ] Update `scripts/development-workflow/README.md` with the new helper,
      subcommands, key fields, JSON output, and examples.
- [ ] Update
      `docs/testing/workflow/1357-delivery-bundle-issue-manifest-workflow.smoke-test.md`
      during implementation if final command names or output keys differ from
      this plan.
- [ ] Update `CHANGELOG.md` under `[Unreleased]` in the implementation PR:
      `- **Add delivery bundle manifest workflow** (#1357): Add hub-owned delivery bundle issue and manifest tooling for coordinated component delivery evidence.`

---

## Testing Strategy

**Test types**: Unit / Integration / Smoke / Manual

**Key scenarios to test**:

1. Create a bundle manifest from required issue-template-style metadata and
   verify revision `1`, status `open`, metadata fields, declared components,
   and audit event. Maps to AC1 and AC2.
2. Add complete component release evidence for one component and verify the
   component entry, stable identity fields, required component tag, optional
   component version, source and release PR references, outcome fields, revision
   increment, and audit event. Maps to AC3.
3. Reapply identical component evidence and verify the helper reports an
   idempotent no-op without duplicating the component entry. Maps to AC4.
4. Inspect a partial bundle and verify missing routing, release PR, CI,
   deployment, cleanup, hub tracker reconciliation, and child release-state
   evidence are reported per component. Maps to AC5.
5. Submit conflicting stable identity, component tag, component version,
   release correlation key, or contract revision evidence and verify the
   manifest file remains unchanged. Maps to AC6.
6. Submit an update or finalization request against a stale expected revision
   and verify the manifest file remains unchanged. Maps to AC7 and AC13.
7. Remove a component before finalization and verify a new revision, removal
   reason, audit event, and preserved prior revision history. Maps to AC8.
8. Move a bundle to `ready_to_finalize`, then add, update, or remove a
   component and verify readiness is invalidated for the current revision. Maps
   to AC9.
9. Attempt finalization with a missing component tag, missing evidence, failed
   or blocked outcome, pending outcome, conflicting outcome, and stale
   readiness; verify each case blocks finalization. Maps to AC10-AC12.
10. Finalize a complete bundle and verify status `finalized`, revision
    increment, finalization audit event, final component list, and absence of a
    shared suite version or shared release branch. Maps to AC13 and AC14.
11. Verify the source `component_release_evidence.v1` files are unchanged by
    create, update, inspect, removal, and finalization commands. Maps to AC15.

**Smoke test runbook**:
`docs/testing/workflow/1357-delivery-bundle-issue-manifest-workflow.smoke-test.md`

**Regression suite**: Add
`scripts/development-workflow/tests/test-delivery-bundle-manifest.sh` and run it
with the existing shell workflow test harness pattern.

### Parser-risk addendum

- **Edge-case enumeration**:
  - Missing, invalid, or unsupported manifest `schema_version`.
  - Malformed JSON, non-object JSON, empty JSON, and missing required bundle
    metadata.
  - Missing, malformed, or mismatched immutable `bundle_key` compared with the
    CLI `--bundle-key` target.
  - Duplicate component identity using the same selected product repository
    key, canonical repository identity, release correlation key, and contract
    revision.
  - Identical evidence replay for an existing component.
  - Conflicting component evidence for stable identity fields, component tag,
    component version, release correlation key, or contract revision.
  - Stale expected revision on component update, removal, inspection readiness,
    and finalization.
  - Failed, blocked, pending, missing, conflicting, and stale outcomes in
    routing, release, CI, deployment, cleanup, hub tracker reconciliation, and
    child release-state fields.
  - Component removal, preserved historical revision, and re-addition requiring
    fresh evidence.
  - Atomic temporary-file validation failure before replacement.
  - Missing lock acquisition, lost-update race, and check-then-create race for a
    new manifest.
  - Structured error propagation for argument validation, JSON parsing, staging,
    schema validation, lock acquisition, and replacement failures.
- **Unit test mapping**:
  `scripts/development-workflow/tests/test-delivery-bundle-manifest.sh` should
  include one named assertion group per edge case above, plus positive coverage
  for create, update, inspect, remove, and finalize flows.
- **Suppression semantics**: Not applicable. The helper validates structured
  manifest and evidence JSON and must not support suppression directives.

### Concurrent-event-source addendum

- **Shared mutable state guards**: No async listener is introduced. Shared
  manifest mutation is guarded by an inter-process exclusive lock, a locked
  current-revision recheck, exclusive temporary-file creation in the manifest
  directory, staged JSON validation, and atomic replacement.
- **Re-entrancy / in-flight tracking**: Not applicable as an event handler. A
  second CLI invocation may contend for the same manifest; after acquiring the
  lock it must fail or retry when the expected revision no longer matches.
- **Event deduplication**: Logical duplicate component evidence is deduplicated
  by stable identity fields and exact accepted evidence replay detection.
- **Listener and resource cleanup**: Not applicable; no listeners, timers, or
  long-lived handles are introduced.
- **Race conditions at initialization**: Bundle creation should fail when the
  target manifest already exists unless the command is an explicit idempotent
  read of the same manifest.
- **Race conditions at teardown**: Not applicable; no teardown lifecycle exists.
- **Error propagation across async boundaries**: Not applicable; shell command
  failures should return non-zero status and stable structured stderr text. The
  helper must validate arguments before mutation; emit `ERROR_CODE=<stable_code>`
  plus a human-readable message; propagate required failures from JSON parsing,
  staging, schema validation, lock acquisition, temporary-file creation, and
  replacement commands; and preserve the previous manifest unchanged on every
  failure. `test-delivery-bundle-manifest.sh` must cover each failure path and
  assert its non-zero exit status and error code.

---

## Seed Data

| Entity | Values / Scenario | File |
| --- | --- | --- |
| Bundle metadata fixture | Delivery title, purpose, parent epic `#1352`, declared components, known child items, finalization owner, and rollout notes | Created under the test temp directory by `test-delivery-bundle-manifest.sh` |
| Complete component evidence fixtures | Two `component_release_evidence.v1` records with component tags, successful release, CI, deployment, cleanup, hub reconciliation, and child state data | Created under the test temp directory, reusing the #1356 fixture style where practical |
| Negative component evidence fixtures | Missing component tag, stale revision, conflicting identity, failed outcome, pending outcome, blocked outcome, and malformed JSON | Created under the test temp directory by `test-delivery-bundle-manifest.sh` |

---

## Documentation Updates

The authoritative documentation checklist is in
**Documentation / Workflow Guides** above. Do not duplicate those entries during
implementation; complete that section and keep the implementation PR diff
aligned with it.

---

## Risks & Mitigations

| Risk | Likelihood | Impact | Mitigation |
| --- | --- | --- | --- |
| Atomicity is implemented as independent file writes, allowing stale component evidence to overwrite accepted bundle state. | Med | High | Require current-revision validation, staged JSON validation, and atomic replacement in one helper path; add stale-write regression tests. |
| Finalization outcome mapping drifts from #1356 evidence enums or release protocol wording. | Med | High | Consume existing `component_release_evidence.v1` fields directly and add tests for every allowed and blocked enum. |
| #1357 absorbs #1358 milestone reconciliation or #1359 adoption behavior. | Low | Med | Keep milestone stamping, rollout adoption, migration, and assurance docs out of this implementation except for explicit out-of-scope notes. |
| The first issue template in the repository becomes too loose for automation. | Med | Med | Keep required fields explicit and mirror them in the helper's `create` validation. |
| Inspection output is readable but not machine-actionable for resumed runs. | Med | Med | Emit stable text and JSON output with status, revision, blockers, and next action. |

---

## Implementation Order

1. Add `.github/ISSUE_TEMPLATE/delivery-bundle.yml` with the required hub-owned
   bundle intake fields.
2. Add `scripts/development-workflow/delivery-bundle-manifest.sh` with schema
   constants, argument parsing, JSON validation, and `create` output.
3. Implement atomic manifest mutation helpers that validate expected revision,
   stage JSON, replace the manifest, and append audit events.
4. Implement component evidence ingestion, stable identity conflict detection,
   idempotent replay detection, and readiness invalidation.
5. Implement `inspect`, blocker reporting, readiness computation, component
   removal with reason, and finalization checks.
6. Add `scripts/development-workflow/tests/test-delivery-bundle-manifest.sh`
   with the positive and negative parser-risk cases listed above.
7. Update repository-mode, release-protocol, and helper README documentation.
8. Run the smoke-test runbook and update it if command names or output keys
   differ from the final implementation.
9. Update `CHANGELOG.md` under `[Unreleased]` with the #1357 implementation
   entry.
10. Run the explicit validation suite before opening the implementation PR:
    targeted shell tests including
    `scripts/development-workflow/tests/test-delivery-bundle-manifest.sh`,
    standard Markdown lint with `npx markdownlint-cli2`, heuristic Markdown lint
    with `python3 scripts/lint/markdown-heuristic-lint.py CHANGELOG.md`, the
    workflow shell snippet guard with
    `python3 scripts/lint/workflow-shell-snippet-lint.py --base-ref origin/develop`,
    ShellCheck for changed shell scripts when available, and
    `git diff --check`.
