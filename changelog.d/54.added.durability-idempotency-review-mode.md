- **Durability and idempotency review mode** (#54): Add implementation-stage review mode with deterministic activation, operator overrides, loop visibility, and PR #51 seed regression fixtures.
- **Durability mode activation and regression oracle** (#54): Treat root-level `webhook/*.ts` as sensitive and require every expected keyword before marking a seeded regression shape as found.
- **Durability mode trust and coverage hardening** (#54): Keep reviewed-head mode text out of the system prompt, retain `duplicate_delivery` for publisher paths, require one finding to carry full shape evidence, and record a stage-specific inactive reason.
- **Durability HTTP path and incomplete-document handling** (#54): Instruct the HTTP local reviewer when mode is active, reject incomplete mode documents as unavailable, and include rename previous paths in workflow activation.
- **Durability rename REST metadata and severity oracle** (#54): Read rename previous paths from the pull-files REST API and require expected severity when classifying regression fixtures.
- **Durability loop metadata forwarding** (#54): Forward `REVIEW_DURABILITY_*` keys through the local-ai-reviewer wrapper and slurp paginated pull-files responses before rename parsing.
- **Durability rename fail-closed and regression harness** (#54): Escalate when rename metadata cannot be read, require findings to target the fixture path, and bound live regression model requests with `passTimeoutMs`.
- **Durability inactive history and per-shape timeout** (#54): Persist `inactive_reason` in reviewer-loop history and apply `passTimeoutMs` per regression fixture rather than across the whole suite.
- **Durability mode heading line anchoring** (#54): Treat required scenario-family headings as complete only when they appear as Markdown heading lines, not as prose substrings.
- **Durability keyword case and fenced headings** (#54): Match queue/retry/idempot paths case-insensitively in shell, and ignore fenced code when checking mode-document completeness.
- **Durability reviewed-head guidance and ledger reset** (#54): Load the mode document from the reviewed commit, clear stale durability history globals when a round emits no state, and fix adoption-doc trailing whitespace.
- **Durability Action document fallback and workflow inputs** (#54): Fall back to the deployed Ronda checkout mode document when the consumer PR head lacks it, and expose `durability_mode` / `durability_mode_default` reusable-workflow inputs.
- **Durability self-review no local fallback** (#54): When reviewing `lhpaul/ronda`, a missing mode document at the reviewed head stays `unavailable` instead of loading the checkout copy.
- **Durability regression scope and durable-store paths** (#54): Keep duplicate-delivery fixtures on delivery-sensitive paths, fail the benchmark when forced mode is not active, and treat durable/persist path keywords as sensitive.
- **Durability test harness and unreadable local fallback** (#54): Mock pull-files / origin/develop in local-ai-reviewer tests, plant forge-URL and development-path lint fixtures, and preserve `unreadable` when the local mode document cannot be read.
