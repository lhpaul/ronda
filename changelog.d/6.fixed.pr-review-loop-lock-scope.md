- **Repository-scoped reviewer-loop lock and runnable Step 8a review-thread gate** (#6): `pr-review-loop.sh` now keys its single-instance lock by target repository *and* PR number (`/tmp/pr-review-loop-<repo-key>-<pr>.lockdir`, where the key carries both a readable owner-repo label and a digest so distinct slugs cannot fold onto one lock) instead of by PR number alone, so a live loop in one checkout no longer blocks a same-numbered PR in another with `REASON=lock_contention`. `unlock` accepts the same `--repo`/`--product-repo`/`--repo-root` selectors, contention output carries `LOCK_REPO_KEY`, and the printed recovery command names the repository so it cannot delete another repository's live lock. Protocol 91's Step 8a review-thread gate is also runnable again: its GraphQL query had one extra closing brace (`gh api graphql` failed with `actual: RCURLY`) and passed unsubstituted `<owner>`/`<repo>` placeholders, which made the protocol's only authoritative thread-resolution check abort before it inspected a single thread.
