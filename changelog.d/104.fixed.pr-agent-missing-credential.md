- **PR-Agent no longer fails silently without a model credential** (#104): the
  `PR-Agent` workflow now fails at a `Require model credential` step when the
  `DEEPSEEK_API_KEY` repository secret is empty, instead of running to the model
  call, publishing nothing and reporting `success`.
