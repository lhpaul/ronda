- **`/ronda review` comment trigger for this repository, and a corrected adopter
  snippet** (#109): `ronda-review-dogfood.yml` now also runs on `issue_comment`,
  so a collaborator's `/ronda review` comment starts a pass on the current head.
  Comment runs use their own concurrency group, so an unrelated comment can no
  longer cancel an in-flight pass or displace a queued one. The adoption doc's
  caller snippet gets the same fix and now states that the caller job must not be
  named `Ronda review`, which made the pass skip itself and publish nothing.
