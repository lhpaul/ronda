- **Ronda v0 GitHub review** (#2): a reusable GitHub Actions workflow posts
  one comment-only review and one check run per head SHA on ready pull
  requests, with findings from a configurable OpenAI-compatible model vendor.
  The pass deadline bounds the whole GitHub phase (not only the model call),
  every GitHub read is under the same bounded retry policy, and a check-run
  write failure after a successful review is retried and surfaced without
  ever contradicting the review a reader can already see.
