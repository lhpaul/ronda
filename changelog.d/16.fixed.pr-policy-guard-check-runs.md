- **Prevent stale PR policy check-runs from blocking readiness** (#16): early
  PR lifecycle policy runs now keep their workflow check green when the
  reviewer-loop summary is expectedly absent, while still posting the failing
  reviewer-loop guard status that blocks readiness until the loop is clean.
  Protocol 91's readiness checklist now deduplicates check-runs by check key so
  it agrees with `pr-ci-loop.sh` on the current head verdict.
