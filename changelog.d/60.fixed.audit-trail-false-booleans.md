- **Explicit `false` policy values now appear in `/run-epic` audit records** (#60): the
  PR-disposition "Invocation Policy" table and the epic ledger's "Effective policy:" note
  rendered a recorded `false` (for example from `--no-may-merge`) as a blank value, making
  it indistinguishable from a policy field that was never recorded. Both renderers now
  print the literal `false`, while a genuinely absent field still renders empty.
